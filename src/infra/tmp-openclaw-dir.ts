// Compatibility shim for older import paths that still reference the
// pre-fork helper name. The implementation lives in tmp-laia-arch-dir.ts.

export { POSIX_LAIA_ARCH_TMP_DIR, resolvePreferredOpenClawTmpDir } from "./tmp-laia-arch-dir.js";
